# todo
1. make the navbar left aligned
2. the window doesn't drag from the navbar, fix this
3. border colors of many elements are still the old color scheme, change them to match the new one
4. the timeline is hard to see, make it more visible
5. change the default folder to the last used folder
6. change the default scrollbar to match the new color scheme and have a cleaner look